# Sample Mosquitto config for a broker installed on your PC (not used by any container in this repo).
listener 1883
allow_anonymous true

# For production, disable anonymous auth and use password_file or plugin.
